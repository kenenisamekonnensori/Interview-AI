# Local infrastructure

Run `docker compose up -d` from the repository root to start PostgreSQL and Redis.

See [queue operations](../docs/operations.md) for production worker retry, recovery, and shutdown procedures.

See [database and storage operations](../docs/database-operations.md) for migration, backup, restore, cleanup, and account-offboarding procedures.

See [production deployment](../docs/deployment.md) for the Docker-compatible service topology, environment inventory, release flow, and rollback guidance.
