# Web application

This is the independently deployable Next.js frontend.

Deploy it on Vercel with `apps/web` selected as the project root directory. The application consumes shared workspace packages and communicates with the API through typed contracts as those modules are introduced.
