import { PlaceholderSection } from "@/features/dashboard/components/placeholder-section";

export default function SubscriptionPage() {
  return (
    <PlaceholderSection
      eyebrow="Subscription"
      title="Subscription management is coming soon"
      description="When plans launch, your plan, usage, and billing will be managed here. Until then, every feature you see is available in early access."
    />
  );
}
